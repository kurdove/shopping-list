<!DOCTYPE html>
<html lang="en">
    <head>
        <% include ./partials/head.ejs %>
    </head>
    
    <body>
        <% include ./partials/navbar.ejs %>

        <main class="container">
            <% include ../static/partials/messages.ejs %>

            <header class="jumbotron my-4 horizontal-center">
                <h1 class="display-3">Welcome to ShopList</h1>
                <p class="lead">A grocery list web-application that can be shared in real-time by multiple people.</p>
                
                <% if(currentUser) { %>
                    <a href="/users/sign_out" class="btn btn-primary btn-lg">Sign out</a>
                <% } else { %>
                    <a href="/users/sign_up" class="btn btn-primary btn-lg">Sign up</a>
                    <a href="/users/sign_in" class="btn btn-primary btn-lg">Sign in</a>
                <% }%>
            </header>

            <section class="row text-center">

            <section class="col-lg-4">
                <h4 class="card-title">Lists</h4>
                <p class="card-text">Create shopping list categories.</p>
            </section>

            <section class="col-lg-4">
                <h4 class="card-title">Items</h4>
                <p class="card-text">Add, delete update items of your chose.</p>
            </section>

            <section class="col-lg-4">
                <h4 class="card-title">Groups</h4>
                <p class="card-text">Create groups and invite people to join them.</p>
            </section>

            </section>

        </main>

        <% include ./partials/baseScripts.ejs %>
    </body>
</html>